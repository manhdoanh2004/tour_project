nav.sider
  ul.inner-menu
    if (permissions.includes("dashboard-view"))
      li
        a(href=`/${pathAdmin}/dashboard` class="") <i class="fa-solid fa-gauge-high"></i> Tổng quan
    if (permissions.includes("category-view"))
      li
        a(href=`/${pathAdmin}/category/list`  class="" ) <i class="fa-solid fa-table-cells-large"></i> Quản lý danh mục
    if (permissions.includes("tour-list"))
      li
        a(href=`/${pathAdmin}/tour/list`  class="") <i class="fa-solid fa-table-list"></i> Quản lý tour
    if (permissions.includes("order-list"))
      li
        a(href=`/${pathAdmin}/order/list` class="") <i class="fa-solid fa-list-check"></i> Quản lý đơn hàng
    if (permissions.includes("user-list"))
      li
        a(href=`/${pathAdmin}/user/list` class="") <i class="fa-solid fa-user"></i> Quản lý người dùng
    if (permissions.includes("coupon-list"))
      li
        a(href=`/${pathAdmin}/coupon/list` class="") <i class="fa-solid fa-ticket"></i> Quản lý phiếu giảm giá
    if (permissions.includes("contact-list"))
      li
        a(href=`/${pathAdmin}/contact/list` class="") <i class="fa-solid fa-user-group"></i> Thông tin liên hệ
  hr
  ul.inner-menu
    if (permissions.includes("setting-list"))
      li
        a(href=`/${pathAdmin}/setting/list`) <i class="fa-solid fa-gear"></i> Cài đặt chung
    li
      a(href=`/${pathAdmin}/profile/edit`) <i class="fa-solid fa-user-gear"></i> Thông tin cá nhân
    li
      a(href="javascript:;" class="inner-logout") <i class="fa-solid fa-power-off"></i> Đăng xuất
.sider-overlay